%h1 betwext keyword / number list
%ul
  - @betwext_entries.each do |sms|
    %li
      = sms.sender_number
      - counter = 0
      - [4540, 4541, 4542, 4543, 4544].each do |list_id|
        - counter += 1
        - if sms.betwext_winners.first(:betwext_list_id => list_id)
          %strike round #{counter}
          |
        - else
          %a{ :href=> "/api/betwext/add_to_betwext_list/#{sms.keyword}/#{list_id}/#{sms.sender_number}" }round #{counter}
          |
